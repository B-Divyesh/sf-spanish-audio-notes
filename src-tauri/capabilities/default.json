{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required for selecting and playing local audio.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
